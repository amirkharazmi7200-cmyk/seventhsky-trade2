name: Seventh Sky Market Radar Poller (legacy disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy poller is disabled. The v3 discovery-only radar is the active scheduled workflow."
