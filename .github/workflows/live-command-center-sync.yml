name: Live Command Center Gmail Feed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-check:
    runs-on: ubuntu-latest
    steps:
      - name: Manual check
        run: echo "Legacy Gmail feed consumer disabled; Command Center inbox is handled by gmail-to-command-center.yml"
